const emailAdress = {}
var email = prompt("enter email");
function email(gmail) {
   for (let i = 0; i < emailAdress.length; i++) {
    const emailAdressS = email[i].split(".");
    str = gmail.replaceAll("", ".");
    if (emailAdress) {
        
    }
 }
   return "this is the correct email address:" + email ;
}
console.log(email);


//corection le "17.01.23"//