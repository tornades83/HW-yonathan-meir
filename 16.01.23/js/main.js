const emailAdress = {}
var email = prompt("enter email");
function email(gmail) {
   for (let i = 0; i < gmail.length; i++) {
    const gmail = props[i].split(".");
    str = str.replaceAll("", ".");
    return "this is the correct email address:" + email ;
    
   }
}
console.log(email);